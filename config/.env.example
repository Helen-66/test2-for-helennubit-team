# .env 配置示例
# 复制为 .env（该文件已被 .gitignore 忽略，不会提交），用于本地敏感信息。
# 使用 APITEST_ 前缀的变量会覆盖配置文件；双下划线表示嵌套层级。

APITEST_BASE_URL=https://api.example.com
APITEST_TIMEOUT=30

# 敏感信息：token / 密钥，仅存于本地环境，不会输出到日志
APITEST_AUTH__TYPE=bearer
APITEST_AUTH__TOKEN=your-secret-token-here
APITEST_API_KEY=your-api-key-here
